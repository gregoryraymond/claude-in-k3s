Claude Code Helm chart deployed.

Namespace: {{ .Values.namespace }}
Projects: {{ len .Values.projects }}

{{- range .Values.projects }}
  - {{ .name }} ({{ .path }})
{{- end }}

Check pod status:
  kubectl get pods -n {{ .Values.namespace }}

Exec into a project pod:
  kubectl exec -it -n {{ .Values.namespace }} deploy/claude-<project-name> -- claude -p --dangerously-skip-permissions "your prompt"

View logs:
  kubectl logs -n {{ .Values.namespace }} -l app.kubernetes.io/name=claude-code -f
